export const AI_PROVIDER = "openai";
export const MODEL_NAME = "gpt-future-model";
export const API_MODE = "backend-proxy";
export const USE_LOCAL_SIMULATION = true;

// No incluyas claves API en este archivo ni en el navegador.
// Cuando haya IA real, crea un backend que lea las claves desde variables de entorno.
